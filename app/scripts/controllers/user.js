'use strict';

/**
 * @ngdoc function
 * @name bluesoftApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the bluesoftApp
 */
angular.module('bluesoftApp')
  .controller('UserCtrl', function ($scope) {
  	$scope.username = 'test'
  });
